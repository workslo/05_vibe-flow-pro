/**
 * In this template, we are redirecting the root path to the workflow editor.
 * This is done to better organize the application structure and makes it easier
 * to copy paste the code into another Next.js project.
 */

export default async function Home() {
  return <div>your index page</div>;
}
